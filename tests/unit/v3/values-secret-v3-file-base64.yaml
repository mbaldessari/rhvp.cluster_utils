version: "3.0"

settings:
  targets: ["hub"]
  namespace: "validated-patterns-secrets"

secrets:
  certificates:
    ca_cert: "file://test-file-contents"
    binary_cert: "file+base64://test-file-contents"

  mixed-config:
    username: "testuser"
    password: "generate:basic"
    config_file: "file+base64://test-file-contents"