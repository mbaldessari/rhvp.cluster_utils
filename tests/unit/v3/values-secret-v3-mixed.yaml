version: "3.0"

settings:
  targets: ["hub", "spoke1"]
  namespace: "validated-patterns-secrets"

secrets:
  database:
    username: "dbuser"
    password: "generate:strong"
    ca_cert: "file://test-file-contents"

  api-config:
    targets: ["spoke1"]
    endpoint: "https://api.example.com"
    token: "generate:medium"