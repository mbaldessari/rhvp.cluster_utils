version: "3.0"

settings:
  targets: ["hub", "spoke1", "spoke2"]
  namespace: "my-app-secrets"

secrets:
  app-config:
    database_url: "postgresql://user:pass@localhost/db"
    api_key: "myapikey123"